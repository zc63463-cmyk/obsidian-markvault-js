# Handoff: MarkVault-JS Obsidian 批注插件 — v5.8 图谱功能全面升级

## Current state

### 已完成（v5.5 ~ v5.8，本轮对话）

**图谱视觉优化** (v5.5 → v5.6):
- per-link 动态距离 `35 + labelLen*6`（上限160），per-node 斥力 `-80 - degree*15`，forceCollide 碰撞检测
- 字体 `10/sqrt(globalScale)` ∈ [6,12]，三档缩放自适应标签截断
- `refresh()` 末尾延迟 500ms 自动 zoomToFit

**图谱筛选面板 7 维度** (v5.7):
- 搜索框（模糊匹配高亮）、关系类型 tag chips、标注类型 tag chips、文件路径下拉、邻居深度滑块 (0~5)、孤立/无效 toggle、实时统计
- 后端：bfsReachable() 邻居深度筛选 + showInvalidated 过滤
- 三行 toolbar 布局（搜索+按钮 / 关系+类型+toggle / 深度+文件+统计）

**关系类型覆盖补全 9→16** (v5.8):
- 新增 7 种主动类型：elaborates/exemplifies/illustrates/causes/enables/precedes/part-of
- 新增 7 种被动类型：isElaboratedBy/isExemplifiedBy/isIllustratedBy/isCausedBy/isEnabledBy/follows/contains
- 修复 RELATION_PALETTE 旧错误（supports/contradicts/isContrastedBy 已删除）
- 修复 CSS chip 颜色 ID 全错问题（旧 cite/support/refute/extend/relate → 全量 16 种真实 ID）

### 测试状态
- metadata: 73/73 ✅
- graph: 17/17 ✅
- search: 119/119 ✅
- store: 17/17 ✅
- w3c-serializer ✅
- w3c-import ✅
- **全部测试通过，已构建并部署到** `E:\Notes\数据库系统概论\.obsidian\plugins\markvault-js\`

### 关键构建约定
- **必须使用**: `node esbuild.config.mjs production`（不能手动 esbuild，否则 @codemirror/* 会被内联打包导致双实例报错）

---

## Pending: 用户反馈待处理

### 🔴 高优先级：筛选面板 toolbar 布局调整

用户查看截图后提出意见：

> "Relation 和 Kind 需要隔离，Kind 单独起一行"

**当前状态**: Relation tag chips 和 Kind tag chips 混在同一行（`row2`），中间用分隔符隔开。当有 16 种关系类型后，这一行会非常拥挤。

**需要做的**:
1. `RelationGraphView.ts` 的 `renderToolbar()` 方法中，将 Relation 和 Kind 拆分为独立的两行
2. 建议新布局：
   - **第 1 行**: 标题 + 搜索 + 按钮（保持）
   - **第 2 行**: Relation: [16 个 tag chips] + Isolated toggle + Invalid toggle
   - **第 3 行**: Kind: [inline] [block] [span] [region]（独立一行，呼吸感更好）
   - **第 4 行**: Depth 滑块 + File 下拉 + 统计（保持）
3. 可能需要调整 CSS：chips 行支持换行 (`flex-wrap: wrap`)，每行有独立标签前缀
4. King 单独一行时去掉两侧分隔符，整体更清爽

**涉及文件**:
- `src/ui/graph/RelationGraphView.ts` — `renderToolbar()` 方法（约第 130~290 行）
- `styles.css` — `.markvault-graph-filter-row` 相关样式

---

## Relevant artifacts

- `.workbuddy/memory/MEMORY.md` — 项目长期记忆（概况、版本、待办）
- `.workbuddy/memory/2026-06-16.md` — 本轮对话详细日志
- `.workbuddy/overview.md` — v5.8 实施总览
- `src/types/annotation.ts` — 30 条内置关系类型配置（16 active + 14 passive）
- `src/ui/graph/RelationGraphView.ts` — 图谱主视图（~820 行），包含 toolbar、drawNode、applyAdaptiveForces、搜索高亮
- `src/ui/graph/graph-data-builder.ts` — 图谱数据构建（含 bfsReachable、RELATION_PALETTE）
- `src/ui/graph/graph-types.ts` — GraphFilter/GraphNode/GraphLink 类型定义
- `styles.css` — 图谱筛选面板样式（chip 颜色、滑块、下拉等）
- `esbuild.config.mjs` — **构建入口**，必须用此文件构建

### 部署路径
- 源码: `E:\Development\MyAwesomeApp\markvault-js\`
- 部署目标: `E:\Notes\数据库系统概论\.obsidian\plugins\markvault-js\`

---

## Next steps

1. **Toolbar 布局调整** (最高优先级) — Relation / Kind 分行隔离，16 种 chip 分行后体验更好
2. **Phase 4C 待办**（来自 MEMORY.md）:
   - P2-1: main.ts 3581行拆分
   - P2-2: annotation-store.ts 2400行拆分
   - P2-3: window.confirm → Obsidian Modal
   - P2-4/P2-8: 清理 debug 测试 + 简化 MOTIVATION_MAP

---

## Suggested skills

- `handoff` — 如果下次还需要交接
- `code` — 实现 toolbar 布局调整时使用
- `diagnose` — 如有 UI 渲染问题排查
- `verification-before-completion` — 改动完成后跑测试验证
