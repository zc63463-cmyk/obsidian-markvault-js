# 2025-06-15 Phase 2 关系图谱审计 + P1~P3 修复

## 审计发现
- P1-1: 预设颜色ID未解析 → dimColor/glow 崩溃
- P1-2: 目标节点重复入组 → nodes[] 含重复项
- P2-1: 无 ResizeObserver → 窗口缩放不跟随
- P2-2: handleNodeHover O(n) → 大图卡顿
- P2-3: GraphFilter 与 RelationGraphView 循环依赖
- P2-4: graph 测试未纳入 npm test
- P2-5: 硬编码亮色值无主题适配
- P2-6: linkPairs 死代码
- P2-7: 注解变更后图谱不自动刷新

## 修复完成
1. **resolveColor()** — PRESET_COLORS 映射表 + resolveColor 函数
2. **nodeMap.has() 检查** — 主循环和目标节点创建都去重
3. **ResizeObserver** — 窗口缩放自适应
4. **adjacencyMap** — 预建邻接表 O(1) hover 查询
5. **graph-types.ts** — GraphFilter 提取到独立类型文件
6. **package.json** — graph 测试加入 npm test（去掉无效 NODE_OPTIONS=）
7. **detectTheme()** — 读取 body.classList 适配暗/亮主题
8. **refreshSidebar → refreshGraphView** — 自动刷新
9. **destroyForceGraph()** — try-catch 包裹 _destructor
10. **uuidSet/uuidMap** — 替代 O(n) some()/find()

## 测试结果
- 244 tests all passing (原有 242 + 2 新增: preset color ID + target node dedup)
- TypeScript 编译 0 errors
