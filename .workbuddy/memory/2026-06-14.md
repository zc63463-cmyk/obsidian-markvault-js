# 2026-06-14 工作日志

## 完成事项

### Region 架构重构（基于 Block 架构重写）✅
- 用户澄清：Region 应为 Block 的异化版本，而非反过来改造 Region 去模拟 Block
- 重写 `processRegionAnnotations` — 移除 DOM 扫描，改为：parseRegionAnnotations(源码) → collectLeafBlocks + computeBlockStarts → 块级背景渲染
- **修复关键 bug**：Region 锚点是内联的（`-%%markvault-region:...:start/end%% 正文`），不能用 `^...$` 按行匹配。改用已有的 `parseRegionAnnotations` 解析全文件内容
- 修复 migration.ts 构建问题：dexie 模块声明 + any 类型替代
- 修复 npm test 命令：添加 NODE_OPTIONS= 前缀
- 验证：tsc 0 error | esbuild 构建成功 | 43/43 测试通过

### Bug #5.1 修复: 编辑模式 region 偶尔不显示 ✅
- 诊断根因：CM6 layer 渲染与 region 缓存更新的异步竞态条件
- 实现 `regionCacheUpdatedEffect` StateEffect 强制 layer 重绘
- 实现 `requestRegionLayerRedraw()` 在缓存更新后通知 CM6
- 实现 `updateRegionCacheImmediately()` 预填充缓存避免首次渲染为空
- 在 `active-leaf-change` 和 `onFileOpen` 中注入 `activeEditorView`

### Bug #5.2 修复: 阅读模式 region 范围不准 ✅
- 诊断根因：跨 section 的 region start/end Comment 不在同一个 DOM el 中
- 重写 `processRegionAnnotations` 支持 5 种场景（A-E）
- 新增 6 个辅助方法处理跨 section 渲染

### 测试套件验证 ✅
- 确认 3 个测试文件共 34 项全部通过（之前 vitest 误用导致失败报告）
- 正确运行方式：`npm test`（使用 tsx，非 vitest）
- Build 通过

## 状态
- 全部 4 个任务完成
- 仍有 #5.4（UI/逻辑分叉）待处理

### Bug #5.3 修复: "Annotate this block" 无渲染 ✅
- 诊断根因（编辑模式）：与 #5.1 相同的 CM6 异步缓存竞态 — `replaceRange` 触发 CM6 docChanged → decoration plugin 读取空 block 缓存
- 诊断根因（阅读模式）：创建 block 后未调用 `updateSpanCache`/`updateRegionCache`，切回编辑模式时缓存为空
- 修复：
  1. 添加 `updateBlockCacheImmediately()` 预填充方法
  2. `createBlockAnnotation` 增加 `updateBlockCacheImmediately` 预填充 + `updateSpanCache`/`updateRegionCache` 后刷新
  3. 阅读模式创建 block 后增加缓存刷新
  4. `MarkVaultDecorator.update()` 增加 `regionCacheUpdatedEffect` 监听
  5. 预填充方法均调用 `requestRegionLayerRedraw()`
- Build + 测试 34/34 通过

### 阅读模式右键 block 标注 findBestTextOffset 失效 ✅
- 诊断根因：Obsidian 渲染后选中文本与 markdownToPlainWithMap 生成的 plain 文本存在标点/空格差异导致精确匹配失败；且 markdownToPlainWithMap 未处理代码围栏 ```
- 修复：
  1. `markdownToPlainWithMap` 增加代码围栏 ``` 处理（跳过开闭围栏，保留内部代码文本）
  2. `findBestTextOffset` 重构为 4 层匹配策略：完整匹配 → 首尾片段匹配 → DOM 上下文匹配 → 新增模糊滑动窗口匹配
  3. 新增 `findOffsetByDOMContext()` 提取为独立方法
  4. 新增 `findByFuzzySlidingWindow()` — 按词元拆分选中文本，在 plain 中滑动窗口寻找最相似片段
  5. 新增 `tokenizeForFuzzy()` — 按中英文标点拆分文本为词元
- 已部署到 E:\Notes\数据库系统概论

### 阅读模式右键 Region 标注 findBestTextOffset 失效 ✅
- 诊断根因：`normalizeSelectedText` 把所有空白（含换行）压缩为单个空格，但 `markdownToPlainWithMap` 输出的 `plain` 保留了原始换行符。当 region 跨段落时，规范化后的选中文本在 plain 中找不到匹配
- 附带发现 `markdownToPlainWithMap` 的 3 个 bug：
  1. 加粗 `**...**` 只跳过开标记，闭标记残留为 `*` 在 plain 中
  2. 斜体 `*...*` 只跳过开标记，闭标记未被正确跳过
  3. 删除线/高亮 `==...==`/`~~...~~` 丢弃了内部文本
- 修复：
  1. `markdownToPlainWithMap`：加粗、斜体、删除线/高亮统一改为"保留内部文本，跳过开闭标记"
  2. `findBestTextOffset`：新增 `buildNormalizedPlainAndMap()` 方法，将 plain 也做空白规范化（\s+ → 单空格），生成 normalizedPlain 和 normalizedMap
  3. 所有匹配（完整匹配、片段匹配、DOM 上下文匹配、模糊匹配）都在 normalizedPlain 上进行
  4. 通过 normalizedMap → map 双重映射回溯到源文件偏移
  5. region 路径增加 `updateSpanCache` 调用
- 7 项测试全通过（跨段 region、含格式跨段、单段落基准等）
- 已部署到 E:\Notes\数据库系统概论

### 阅读模式 region 在 <li> 上整块高亮（精度问题）✅
- 症状：对无序列表单条内容创建 region 标注，阅读模式黄色背景覆盖整个 `<li>` 块，而非只高亮选中文本
- 根因（双路径）：
  1. `highlightRegionNodes`（comment 节点路径）：遍历时遇到 `<li>` 块元素直接加 `markvault-region` 背景类 → 整个 `<li>` 染色
  2. `styleRegionBlockAncestor`（fallback 路径）：`wrapTextRange` 正确包裹文本为 `<span>` 后，又找到块祖先 `<li>` 加了完整背景类
- 修复：
  1. `highlightRegionNodes` 新增 `findNearestBlockAncestor()` 辅助方法
  2. 在遍历前检测 start/end 是否在同一块元素内 → 若是，跳过对该块的样式化，只包裹内部文本节点
  3. `styleRegionBlockAncestor` 改为只添加点击事件和元数据（`markvault-clickable`），不再添加 `markvault-region` 背景色类
- 已部署到 E:\Notes\数据库系统概论

### 编辑模式 region 锚点元数据文本暴露 ✅
- 症状：编辑模式下显示 `%markvault-region:UUID:type:color:start:%src/core/highlight-applier.ts...` 这样的原始锚点文本
- 根因：region 锚点 note 字段包含 `%` 字符（如文件路径），但 `REGION_ANCHOR_REGEX` 中 `([^%]*)` 以 `%` 为终止符 → 正则匹配失败 → `Decoration.replace` 不生效 → 锚点文本暴露
- 修复（3 个文件）：
  1. **`region-annotation.ts`**：`escapeAnchorField()` 增加 `%` → `\p` 转义（之前只转义 `:`）；`decodeAnchorField()` 增加反转向义
  2. **`highlight-applier.ts`**：严格正则后增加防御性兜底——宽松正则捕获损坏/旧版锚点并隐藏
  3. **`main.ts`**：阅读模式 post-processor 新增 `hideLeakedAnchorText()` 遍历 DOM 文本节点隐藏泄漏锚点
- 已部署到 E:\Notes\数据库系统概论

## 状态
- 全部任务完成
- 仍有 #5.4（UI/逻辑分叉）待处理

### 编辑模式 region 锚点渲染修复 v2（彻底修复）✅
- 前版修复（escapeAnchorField + 宽松正则兜底）未能解决问题
- 根因再分析：
  1. `REGION_ANCHOR_REGEX` 使用 `([^%]*)` 匹配 note 字段，note 中含 `%` 时正则完全无法匹配
  2. 宽松兜底正则也使用 `[^%]*`，同样失败
  3. 第一个宽松正则产生部分匹配后加入 `alreadyHidden`，阻止第二个宽松正则的完整匹配
  4. 阅读模式 `applyRegionStyleToSectionPrecise` 用 `buildRegionAnchor`（生成转义后锚点）搜索源文本，与原始未转义锚点不匹配 → 精确匹配失败 → 回退到 `applyRegionStyleToSection` → 给 `<ul>` 等容器块加背景 → 整段列表染色
- 修复（3 个文件）：
  1. **`region-annotation.ts`**：`REGION_ANCHOR_REGEX` 的 note 字段从 `([^%]*)` 改为 `([^\n]*)`，UUID/type/color 从 `([^:%]+)` 改为 `([^:\n]+)`。锚点不跨行，`[^\n]*` 安全匹配到 `%%` 终止符，不受 note 中 `%` 影响
  2. **`highlight-applier.ts`**：移除有缺陷的双宽松正则兜底，替换为基于子串扫描的兜底：找 `markvault-region` 行，向前找 `%` 开头、向后找 `%%` 结尾，补充隐藏未匹配范围
  3. **`main.ts`**：多处修复：
     - `highlightRegionNodes`：`isWithinSameBlock` 时跳过所有块级元素（不仅是 startBlockAncestor），只包裹文本节点
     - `applyRegionStyleToSectionPrecise`：使用 `REGION_ANCHOR_REGEX` 搜索锚点位置（而非 `buildRegionAnchor`），能正确匹配含 `%` 的旧版锚点
     - `applyRegionStyleFromStartAnchor`/`applyRegionStyleToEndAnchor`：同样改用 `REGION_ANCHOR_REGEX`
     - `applyRegionStyleToSection`：识别容器块（含块级子元素如 `<ul>`），只加点击事件不加背景类
     - `applyRegionStyleToMiddleSection`：递归处理容器块子元素，叶子块才加背景
     - `applyRegionStyleToNodes`：同上容器块逻辑
     - `hideLeakedAnchorText`：使用 `[^\n]*?` 替代 `[^%]*`，增加 `mv:i` 模式
- Build 通过

### 标注数据管理系统深度分析 ✅
- 对 AnnotationStore / annotation-repo / migration / modify-guard / file-encoder / markdown-sync / 导出系统做了全面审计
- **持久化评分 8.5/10**：分片 JSON + 7维倒排索引 + 2s防抖写回 + 4层并发保护 + 启动自动修复
- **导出能力现状**：3 个入口（Settings 全量 JSON / BatchBar 过滤+选中 JSON+MD），缺少单文件限定、含上下文、模板化导出
- **导出架构建议**：设计 `ExportEngine` 模块 + `ExportRequest` 接口，支持 source('all'|'file'|'filter'|'selected') + mode('annotations-only'|'with-context') + format('json'|'markdown'|'csv') + groupBy
- 写入 MEMORY.md 关键架构信息

### 深度开发规划与 PCV 评估（v2） ✅
- 完成 Phase 4-7 完整开发规划（overview.md）
- **Phase 4（1.5周）**：Relation + Flag + Group 三项元数据扩展
  - Relation：8 种语义关联，Store 双向索引，Modal/Card/右键 UI
  - Flag：mastery 4档 + priority 4档 + confidence 1-5 + needsCorrection + reviewCount
  - Group：多对多自由分组标签
- **Phase 5（2周）**：Export Engine LLM-ready 导出
  - 4 种格式：JSON Schema / Markdown / CSV / LLM Prompt
  - Export Bundle：一键三文件（数据+卡片+Prompt）
  - Prompt 模板系统（4 个预设 + 用户自定义）
  - MarkVault Data Protocol v1.0
- **Phase 6（2-3周）**：D3 图谱 + 动态摘要 + AI 回注 + 统计增强
- **Phase 7（3-4周）**：独立门户 SPA
- **PCV 评估**：20/24 Exceptional — Build NOW
- 关键架构决策：Relation/Flag/Group 均仅存 Store；导出独立模块配置驱动；AI 纯管道模式

### Phase 4 实现：知识元数据层 ✅
- **annotation.ts**：新增 AnnotationRelation（8 种 RelationType + label 映射）、AnnotationFlag（mastery/priority/confidence/needsCorrection/lastReviewedAt/reviewCount）、groups: string[]；扩展 AnnotationFilter（mastery/reviewPriority/hasRelations/group/needsCorrection）；扩展 AnnotationStats（withRelations/withGroups/withFlags/byMastery/byReviewPriority/needsCorrection）
- **annotation-store.ts**：新增 5 个索引（_byRelationOut/_byRelationIn/_byGroup/_byMastery/_byReviewPriority）；更新 _addToIndex/_removeFromIndex/_stripExtraFields/rebuildIndex/initialize/queryAnnotations/getAnnotationStats；新增 API：addRelation/removeRelation/getRelations/updateFlags/addGroupToAnnotation/removeGroupFromAnnotation/getGroupNames
- **annotation-repo.ts**：新增代理方法 addRelation/removeRelation/getRelations/updateFlags/addGroupToAnnotation/removeGroupFromAnnotation/getGroupNames
- **annotation-modal.ts**：新增 Flags 区（Mastery/Priority/Confidence/NeedsCorrection 下拉和开关）、Groups 区（标签式 + Add Group 按钮）、Relations 区（列表 + Add Relation 按钮 + 删除按钮）；save 方法扩展 groups 和 flags 持久化
- **AnnotationCard.ts**：新增元数据徽章区（🔗 relation count / 🎯 mastered / ⚠️ needsCorrection / confidence / priority emoji / group tags）
- **FilterBar.ts**：新增第四行元数据过滤（Mastery 下拉 / Group 下拉 / 🔗 hasRelations / ⚠️ needsCorrection）
- **测试**：新增 tests/metadata-extension.test.ts（20 项测试覆盖 Relation CRUD + Flag CRUD + Group CRUD + 过滤 + 持久化 + 索引一致性）
- **全量测试**：63/63 通过（17 store + 10 native + 7 region + 9 block + 20 metadata）
- **Build**：tsc 0 error + esbuild production 成功，已部署

### Phase 4 CSS 补全 + Relation Picker 样式 ✅
- styles.css 补全 Groups/Relations/Flags/Fields 全部 CSS（~170行）
- styles.css 新增 RelationPickerModal CSS（~100行）

### Phase 4.5 搜索系统重构 ✅
- **审计发现**：Store.queryAnnotations 和 Sidebar.applySearchFilter 两套独立过滤实现，applySearchFilter 缺失 Phase 4 过滤器
- **新建 6 文件**：filter-engine.ts (纯函数)、tokenizer.ts (CJK bigram)、types.ts (SearchRequest/Result/Suggestion)、search-engine.ts (倒排索引)、relation-picker-modal.ts (取代 prompt)、tests/search.test.ts (34项)
- **修改 7 文件**：annotation-store.ts、AnnotationSidebar.ts、annotation-modal.ts、plugin-interface.ts、main.ts、styles.css、package.json
- **测试**：search 34/34 + 存量 5 文件全通过 = 总计 97 项

### 搜索系统深度审查 + 6 项修复 ✅
- **P0 Bug #1: filter-engine 输入变异** — 改为 `[...annotations]` 始终拷贝
- **P0 Bug #2: suggest() 排序缺陷** — `[...scoredUuids.entries()].sort()` 后再取 limit
- **P1 冗余**: search() 中 scope 过滤被执行两次 → 删除
- **P1 CJK 标点污染**: tokenizer 移除 0x3000-0x303F 和 0xFF00-0xFFEF
- **P2 RelationPicker**: `tagEl` 未使用 → 清理
- **测试**: +15 项

### 搜索系统第三轮审查 + 9 项修复 ✅
- **P0 搜索语义不一致**: filter-engine 用 includes() 子串 vs SearchEngine 用 bigram 倒排索引 → 结果不同。修复: filter-engine 改用 tokenizer 分词匹配
- **P0 note null 安全**: `a.note.toLowerCase()` 在 note=undefined 时崩溃 → 改为 `a.note || ''`
- **P0 group 'all' 误过滤**: `filter.group as string` 在 group='all' 时仍过滤 → 先判 `filter.group !== 'all'` 再提取
- **P0 搜索精度**: tokenizer OR 语义下搜"批注"匹配"标注"（单字"注"命中）→ 改为 AND 语义：bigram 必须全部命中 + other token 任一命中
- **P1 批量过滤**: search-engine 对每个候选逐条调 `applyUnifiedFilter([ann])` → 改为先收集全部候选再一次性调 `applyUnifiedFilter(candidates)`
- **P1 _ensureIndex 优化**: 每次 search 都调 `getAllAnnotations().length` → 改为 dirty 时直接重建，非 dirty 时才检查计数
- **P1 空字段防御**: `_countFieldHits` 中 tags/groups/fields 空值时跳过 → 加 `if (t)` / `if (v)` guard
- **P2 连字符**: tokenizer 中纯连字符 '--' 不产生空 token → 加 wordBuffer.length===0 检查
- **测试补充**: +7 项（filter vs engine 一致性/null note safety/group='all'/纯连字符/UUID+CJK+数字/批量过滤50条）
- **测试结果**: search 56/56 + 存量 63 = 119 项全通过
### 搜索系统第四轮全量审查 + 10 项修复 ✅
- **P0 双重过滤冗余**: search-engine 中 `_passesIndexFilters` 逐条过滤 type/color/mastery 后，`applyUnifiedFilter` 又做一遍。修复: 删除 `_getActiveIndexKeys` + `_passesIndexFilters` 两个方法（-35行），完全依赖 `applyUnifiedFilter` 批量后过滤
- **P1 isBigram 范围不一致**: search-engine 的 isBigram 只检查 0x4E00-0x9FFF，但 tokenizer 的 CJK_RANGES 包含 Extension A/B/C。修复: 提取共享 `isCJK()` 函数，与 tokenizer 的 CJK_RANGES 完全一致
- **测试补全**: +5 项 (CJK Extension A / tokenizeQuery alias / CJK bigram 非基础字符 / sortByRelevance false / no-query filter-only)
- **测试结果**: search 61/61 + 存量 63 = 124 项全通过
- **部署**: 已 cp 到 vault

### 搜索系统最终审查收尾 ✅
- **代码正确性修复**：
  1. `isCJK()` 从 tokenizer 导出，filter-engine 和 search-engine 统一导入，消除 CJK_RANGES 重复定义
  2. filter-engine CJK bigram 判断改用共享 `isCJK()`（覆盖 Extension A/B/C），之前只检查 0x4E00-0x9FFF
  3. search-engine 删除内联 CJK_RANGES 和本地 isCJK()，改为从 tokenizer 导入
  4. `_ensureIndex` 数量检测从 `getAllAnnotations().length`（O(n)）优化为 `getAnnotationCount()`（O(1)）
  5. 新增 `AnnotationStore.getAnnotationCount()` 方法返回 `_byUuid.size`
  6. search-engine 无搜索词路径增加 sortBy 排序（之前 scope=file 路径未排序）
  7. 搜索语义统一为 bigram OR + other OR（与 filter-engine 一致），解决跨词边界 bigram 匹配问题
- **RelationPickerModal 修复**：
  - `selectedType` 默认值从 `'references'` 改为 `null`，下拉默认空选项，强制用户主动选择关系类型
  - Link 按钮初始禁用，`selectedUuid && selectedType` 同时满足才启用
  - 新增 `_updateLinkBtnState()` 方法统一管理按钮状态
- **测试覆盖扩充**：+21 项（17→82）
  - Tokenizer: +5（Extension B、Emoji、findMatchSnippet 边界3项、surrogate pairs）
  - Filter Engine: +8（undefined type/color、empty fieldFilters、Extension A bigram search、bigram OR 语义、other OR 语义、whitespace-only、special chars）
  - Search Engine: +8（bigram OR in suggest、sortBy 无搜索词、count change 检测、mixed CJK+English suggest、single-char CJK suggest、scope no-query、matchSnippets）
- **全量测试**：6 文件 145 项全部通过（17+10+7+9+20+82）
- **TypeScript**: 自有代码零类型错误（3 个 obsidian.d.ts SDK 声明问题不影响构建）
- **esbuild**: production 构建成功
- **搜索系统代码量**：5 源文件 1049 行 + 测试 789 行

### 热门搜索项目调研 + 升级方案 ✅
- 调研 MiniSearch（6k★）/ Orama（10.4k★）/ FlexSearch（12k★）三家全文搜索库
- 对比分析 12 个关键维度：BM25 评分、模糊搜索、前缀搜索、索引持久化、短语搜索、Facets、上下文距离评分、Stop Words、向量搜索、Worker 并行、高亮信息、CJK 支持
- **MV-JS 优势**：CJK 分词达到行业先进水平（Extension A/B/C 覆盖、bigram OR 语义、共享 isCJK）
- **8 项改进提案**，优先级排序：
  - P1: BM25 评分（含 IDF + TF saturation + 长度归一化）
  - P1: 英文模糊搜索（Levenshtein 编辑距离）
  - P2: 前缀搜索 / 索引持久化
  - P3: 短语搜索 / Facets
  - P4: 上下文距离评分 / Stop Words
- 决策：不引入向量搜索（离线场景不适配）、Worker（数据规模不需要）、RAG 管道（Phase 5 已覆盖）
- 产出：search-system-analysis.md

### P1 实现：BM25 评分 + 英文模糊搜索 ✅
- **BM25 评分**（search-engine.ts）：
  - 新增 `_docLengths: Map<string, number>`（标注 token 总数）和 `_avgDocLength`
  - IDF = ln((N - df + 0.5) / (df + 0.5) + 1) — 稀有 token 权重高
  - TF saturation: (freq × (k1+1)) / (freq + k1 × (1-b+b × docLen/avgDL))，k1=1.5, b=0.75
  - 字段权重改为 multiplier 模式（1 + weight/100 × 5），BM25 主导排序，字段作为次要信号
  - `scoringModel?: 'weighted' | 'bm25'` — 默认 'bm25'，suggest() 保持 'weighted'
- **英文模糊搜索**（search-engine.ts）：
  - Levenshtein 编辑距离（滚动数组 DP + 行最小值提前终止 + 长度差异提前判断）
  - 仅对长度 ≥3 的非 CJK token 启用，CJK bigram 跳过（长度短无模糊意义）
  - 性能优化：首字符过滤 + 长度 ±maxDist 过滤（约 70%+ 剪枝率）
  - `fuzzy?: number`（0~1）控制编辑距离比例；`fuzzyMaxExpansions` 每 token 上限（默认 5）
  - 模糊命中分数 ×0.85 折扣
- **types.ts** 扩展：SearchRequest 新增 scoringModel、fuzzy、fuzzyMaxExpansions
- **测试**：+14 项（BM25 5项 + Fuzzy 9项），82→96
  - BM25: 稀有词优先、长度归一化、TF 饱和、默认模型、rare term IDF boost
  - Fuzzy: typo tolerance、CJK 不模糊、分数折扣、默认禁用、混合精确+模糊、编辑距离边界、短 token 跳过、性能剪枝、maxExpansions
- **全量测试**：6 文件 159 项全通过（17+10+7+9+20+96）
- **esbuild** production 构建成功
- **代码量**：search-engine.ts 452→669 行（+217），types.ts 61→77 行（+16）

### P2 实现：前缀搜索 + 索引持久化 ✅
- **前缀搜索**（search-engine.ts + types.ts）：
  - `prefix?: boolean` — 对不在索引中的 token，遍历 indexed token 找前缀匹配
  - `_prefixExpand(tokens, outPrefixes)` — 前缀扩展方法：跳过已存在 token、步长过滤、上限 PREFIX_MAX_EXPANSIONS=20
  - 前缀命中分数 ×0.9 折扣，精确匹配优先
  - 适配所有 token 类型（CJK bigram/English/数字），仅扩展未精确命中的 token
- **索引持久化**（search-engine.ts + main.ts）：
  - `exportIndex(): IndexSnapshot` — 导出 Map/Set → JSON 友好结构（invertedIndex + docLengths + indexedCount + avgDocLength）
  - `importIndex(snapshot)` — 从快照恢复全部索引状态（含 BM25 参数）
  - `IndexSnapshot` 接口（types.ts）— version、invertedIndex、docLengths、indexedCount、avgDocLength
  - main.ts 集成：onload 时 `_loadSearchIndex()` 从 `.obsidian/plugins/markvault-js/search-index.json` 读取；onunload 时 `_saveSearchIndex()` 写入
  - 加载失败非致命（降级到延迟重建）
- **测试**：+10 项（前缀 7项 + 持久化 3项），96→106
  - 前缀: English partial、CJK 已有 token 不扩展、CJK novel token、number prefix、分数折扣、默认禁用、prefix+filter 组合
  - 持久化: round-trip、BM25 分数一致性、import 后无冗余重建
- **全量测试**：6 文件 169 项全通过（17+10+7+9+20+106）
- **代码量**：search-engine.ts 669→830 行（+161），types.ts 77→94 行（+17），main.ts +45 行

### 参照 MiniSearch 源码防御性编程模式增强健壮性 ✅
- 分析 MiniSearch v7.2.0 源码全部防御性编程模式（8 大类 50+ 实例）
- **输入校验**：search() 增加 limit 非负检查、fuzzy 0~1 范围检查（throw Error）
- **索引自修复**：倒排索引中 stale UUID 检测 — 每当 `getAnnotationByUuid` 返回 null，标记 `_dirty=true` 并 console.warn，下次 search 自动重建（参考 MiniSearch 搜索时自清理模式）
- **BM25 除零安全**：docLen 和 avgDL 加 `Math.max(value, 1)` 保护（参考 MiniSearch IDF 计算中 `+0.5` 小常数防御）
- **空 token 安全**：`_isEnglishToken()` 增加 `!token || token.length === 0` 守卫子句
- **限制参数防御**：limit=0 返回空数组（而非负 `slice`）
- **测试**：+6 项健壮性测试（112→112），106→112
  - negative limit throws、fuzzy out of range throws、empty token safety、BM25 零长度标注安全、stale UUID self-repair、limit=0
- **全量测试**：6 文件 175 项全通过（17+10+7+9+20+112）
- 参考项目：MiniSearch 的 8 种防御模式

### 搜索 Facets 实现 ✅
- `SearchFacets` 接口 — type / color / mastery / hasNote / noNote 五维分布
- `facets?: boolean` — SearchRequest 可选字段，默认禁用
- `_computeFacets(results)` — O(n) 单次遍历统计
- `lastFacets` getter — 搜索后通过 `engine.lastFacets` 访问
- 非侵入式设计：`search()` 返回 `SearchResult[]` 不变，Facets 通过引擎属性侧通道获取
- **测试**：+7 项（119→119），112→119
  - type distribution、color distribution、mastery distribution、hasNote/noNote、默认禁用、filter+facets 组合、总数一致性
- **全量测试**：6 文件 182 项全通过（17+10+7+9+20+119）

### JSON 存储系统调研 ✅
- 参考 LowDB（原子写入 + adapter 模式）和 LokiJS（内存索引 + 持久化 adapter）
- 当前系统评估：分片 JSON + 12 维内存索引 + 2s 防抖写回 — 架构合理，主要缺写入安全面防御
- **3 项改进提案**（<50行变更）：
  - P0: 原子写入（tmp 文件 + 写入后清理替代 rename）
  - P1: SHA-256 checksum 完整性校验
  - P2: 去掉 `JSON.stringify(data, null, 2)` 的 pretty-print（省 30% 体积）
- 产出：storage-analysis.md

### 存储系统写入安全三项修复 ✅
- **P0: 原子写入** — write-tmp-then-final 模式：先写 .tmp，成功后写目标，最后删 .tmp。Obsidian adapter 不支持 rename，用 write+remove 近似
- **P0: 写入备份** — 写入前将旧文件复制为 .bak，写入失败时保留 .bak 供恢复
- **P1: Checksum 校验** — `_computeChecksum(data)` 纯 JS 多项式 hash（CRC-32 风格），写入时计算 `_checksum` 字段，读取时校验。校验失败 → 自动 fallback 到 .bak 恢复
- **P2: 去 pretty-print** — `_writeFileShard`、`_writeIndexFile`、`_writeMetaFile` 三处 `null, 2` 全部移除
- 读取路径增强：JSON 解析失败也尝试 .bak 恢复
- 影响范围：`_writeFileShard` (+30行) + `_readFileShard` (+15行) + `_writeIndexFile` + `_writeMetaFile` + `_computeChecksum`
- 全量测试 182/182 通过
